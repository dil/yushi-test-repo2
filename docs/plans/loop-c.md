# Plan: loop-c

## Context

This is a small, self-contained exercise plan for exercising the
subagent-driven-development workflow end to end. There is no external spec;
this plan document is the binding authority for what "done" means.

## Global Constraints

- Each task adds exactly one new file at the repo root: `LOOP-<N>.md`.
- Each file contains exactly one line of text (a trailing newline is fine).
- Do not modify any other file (including `README.md`) as part of these tasks.
- Each task is committed separately.

## Task 1: Add LOOP-1.md

Create a file `LOOP-1.md` at the repo root containing exactly one line:

```
Loop marker 1: first light.
```

Commit this file with an appropriate commit message.

## Task 2: Add LOOP-2.md

Create a file `LOOP-2.md` at the repo root containing exactly one line:

```
Loop marker 2: second wind.
```

Commit this file with an appropriate commit message.

## Task 3: Add LOOP-3.md

Create a file `LOOP-3.md` at the repo root containing exactly one line:

```
Loop marker 3: third rail.
```

Commit this file with an appropriate commit message.

## Task 4: Add LOOP-4.md

Create a file `LOOP-4.md` at the repo root containing exactly one line:

```
Loop marker 4: fourth wall.
```

Commit this file with an appropriate commit message.

## Task 5: Add LOOP-5.md

Create a file `LOOP-5.md` at the repo root containing exactly one line:

```
Loop marker 5: fifth gear.
```

Commit this file with an appropriate commit message.

## Task 6: Add LOOP-6.md

Create a file `LOOP-6.md` at the repo root containing exactly one line:

```
Loop marker 6: sixth sense.
```

Commit this file with an appropriate commit message.
